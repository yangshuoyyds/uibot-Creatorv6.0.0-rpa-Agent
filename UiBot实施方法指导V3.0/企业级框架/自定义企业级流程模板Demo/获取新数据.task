/*
获取数据并赋值给流程图变量g_dicTransactionItem，以供流程图全局使用（子流程化需要输入后使用）
获取数据方式可参考：
1、消费1条Commander中指定的的数据队列消息
2、读取指定Excel表格中的特定行
3、读取指定数据库中的指定字段数据
4、读取指定邮箱中的一份邮件
5、通过webAPI获取返回数据
6、其他

*/
g_dicTransactionItem = {} //获取一次新事务的数据前须初始化,此条命令不可删除
If g_bFirstRun
	g_dicTransactionItem["TransactionData"] = [""]
Else
	g_dicTransactionItem["TransactionData"] = Null
End If
g_bFirstRun = False //进入到此流程块内，初次运行状态就需要更新为False,此条命令不可删除


//流程块内有2块区域的命令（非Try/Catch块内包含的命令、Catch块内包含的命令），若发生异常，流程块的逻辑走向会由“异常”虚线分支连线指向到其他流程块进行逻辑处理（子流程内同理）
//流程块内被Try/Catch块内包含的命令，若发生异常（可被称为“业务异常”），只会在Catch块内捕获和处理，不会触发本流程块的“异常”虚线分支连线走向逻辑
//todo something








